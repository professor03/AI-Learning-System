# Gemini API key for local development only
# Do not commit a real key. VITE_* values are exposed in the browser bundle.
VITE_GEMINI_API_KEY=your_gemini_api_key

# Optional LearnSight / YOLO service endpoint
VITE_LEARNSIGHT_API_URL=http://localhost:8000

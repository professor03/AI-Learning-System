# 🚀 AI Student v3.0：基於「第一性原理」的核心重構與技術突破計畫 (Pivot Plan)

這是一份針對專題評審回饋的全面升級計畫。我們將徹底導入伊隆·馬斯克 (Elon Musk) 推崇的**「第一性原理 (First Principles Thinking)」**來重新審視整個專案：**打破所有既有假設，回歸事物最基本的真相，從頭開始建立。**

## 🧠 我們的第一性原理核心考問
> **「學習的本質是什麼？」**
> 學習不是為了看漂亮的心智圖，也不是為了快速產出簡報。
> 學習的本質只有兩個：**1. 將知識牢牢記住 (Retention)**；**2. 能夠解決實際問題 (Application)**。
> 
> 基於這個第一性原理，任何對「記憶留存」與「問題解決」沒有直接幫助的功能，都必須被無情地砍掉；反之，則必須將技術做到極致。

## User Review Required
> [!IMPORTANT]
> 基於第一性原理，此計畫將會**大幅刪減**現有「華而不實」的功能（知識衛星、簡報功能），並將開發資源 100% 投入於底層記憶演算法與檢索架構。請確認您是否同意這個「少即是多 (Less is More)」的重構方向。同意後，我將開始修改程式碼。

## 評審痛點剖析與應對策略

### 1 & 5. 「只是串 API 沒技術含量」&「問答都丟給 API 有什麼技術含量？」
**痛點**：過度依賴大語言模型，系統本身缺乏傳統的運算邏輯或資料結構設計。特別是「問答功能」，如果只是 `prompt + text` 丟給 Google，教授當然會覺得沒有技術挑戰。
**突破策略 (Technical Depth)**：
我們要在專案中加入**兩大非 AI 的硬核演算法**，讓教授無話可說！

*   **突破一：自建本地檢索增強生成引擎 (Custom Local RAG Engine)**：
    *   **您的貢獻與反擊**：「教授，我並不是無腦把整份講義丟給 API。我自己在前端實作了一套 **TF-IDF (或 BM25) 文本檢索演算法**。當學生發問時，系統會先在本地端進行『文本分塊 (Chunking)』與『相似度矩陣計算』，精準計算出關聯度最高的前 3 個段落後，才交由大模型進行總結。這套搜尋引擎的邏輯是我自己寫的，它大幅降低了 Token 的傳輸延遲與成本。」
*   **突破二：導入新世代 FSRS 記憶演算法 (Free Spaced Repetition Scheduler)**：
    *   捨棄早期的 SM-2 演算法。我們將採用目前開源界公認最強的 **FSRS 演算法**，它基於 **DSR 認知模型**（Difficulty 難度、Stability 穩定度、Retrievability 可回憶率）。
    *   **您的貢獻與反擊**：「我用 TypeScript 手工實作了最前沿的 FSRS 演算法邏輯。學生的每一次複習，系統都會進行矩陣運算來預測大腦的遺忘曲線，這是我專題的底層技術突破，而不是依賴 AI。」

### 2. 「跟 MOSME 行動學習一點通有什麼差別？」
**痛點**：缺乏明確的市場差異化。
**突破策略 (Differentiation)**：
*   **MOSME 的本質**：傳統的「題庫系統」與「靜態影音平台」。學生是被動接收（Passive Learning）。
*   **AI Student 的本質**：基於「主動回想 (Active Recall)」與「動態客製化」。MOSME 的詳解是寫死的，看不懂就是看不懂；我們的「知識搶救室」是根據學生錯誤的「當下脈絡」動態生成的。我們要將 UI 徹底向這點收斂，讓它看起來像一個**「嚴格但貼心的教練」**，而不是一個電子書架。

### 3 & 4. 「PBL 問題導向學習」與「如何吸引聰明的學生？」
**基於第一性原理**：知識只有在「被應用」時才會產生真正的理解。
**突破策略 (PBL & Challenge Mode)**：
聰明的學生討厭「被教」，他們喜歡「解謎」與「挑戰」。
*   **實作「專案導向挑戰 (PBL Challenge Mode)」**：在學習完一份講義後，除了傳統選擇題，系統會生成一個「實務情境任務」。
*   *範例*：學完「資料庫」，系統不是問「什麼是 SQL？」，而是給出一個 PBL 任務：「你現在是一間電商的後端工程師，因為結帳系統很卡，老闆要求你優化資料庫，根據講義內容，你會怎麼做？」
*   這種模式可以完美契合高成就學生（讓他們有舞台發揮）與低成就學生（系統會給予鷹架引導）。

### 6. 「刪除不必要的知識衛星與簡報功能」
**基於第一性原理**：觀看浮動的 3D 節點（知識衛星）與生成投影片（簡報工具），是否能直接提升「大腦神經元的連結強度」？答案是：非常微弱。
**突破策略 (Feature Trimming)**：
我們要無情地做減法，這也是高級軟體工程的展現。
*   **移除** `KnowledgeGalaxy.tsx` (知識星系圖)
*   **移除** `PresentationStudio.tsx` (簡報生成器)
*   將導覽列與側邊欄精簡，把「記憶庫 (Memory Bank)」與「任務挑戰 (PBL Challenges)」放到最核心、最顯眼的位置。

---

## 🏗️ 基於第一性原理的系統業務分類 (Business Domain Driven Design)

為了讓專案架構更清晰，並在面試或報告時展現您的軟體架構能力，我們將系統的業務 (Business Domains) 嚴格劃分為以下四大核心層：

### 1. 基礎演算法層 (Core Algorithm Layer)
> **第一性原理**：對抗遺忘必須依靠精確的數學模型與檢索效率。
*   **FSRS 記憶排程引擎**：處理 DSR 模型運算，精準預測下次複習時間。
*   **本地 RAG 檢索引擎**：處理 TF-IDF/BM25 文本分塊與相似度計算，降低 Token 消耗。

### 2. 知識處理與應用層 (Knowledge & Application Layer)
> **第一性原理**：學習必須有高品質的輸入，以及解決問題的輸出。
*   **PBL 任務挑戰模組**：將靜態知識轉化為實務情境任務。
*   **動態測驗引擎 (地獄考場)**：提供名詞解釋、情境應用、反向推論三種難度的試卷生成。

### 3. 動機與行為引導層 (Motivation & Gamification Layer)
> **第一性原理**：學習是反人性的，必須靠心理學與外部誘因來維持行為。
*   **寵物共生系統**：包含等級、血量、以及「48小時未登入扣血」的怠惰懲罰機制。
*   **知識搶救室 (強制閉環)**：不及格時的強制白話文引導與介面鎖定。

### 4. 互動與數據追蹤層 (Interaction & Analytics Layer)
> **第一性原理**：知識必須能被隨時查詢，學習成效必須被量化。
*   **全域情境 AI 聊天室**：隨時感知學生所在頁面與講義脈絡的對話視窗。
*   **學習儀表板紀錄**：量化學習時間、掌握度與寵物狀態的資料追蹤。

---

## 🗺️ v3.0 逐步執行計畫 (Phased Execution Plan)

為了確保專案升級的穩定性，我們將依照四大業務層，由底層到前端，分為四個階段逐步執行：

### Phase 1: 基礎演算法層建置 (Core Algorithm Layer)
這是系統的最底層大腦，不涉及任何 UI，純粹實作數學邏輯。
*   **Step 1.1: 實作 FSRS 引擎**
    *   建立 `src/lib/fsrs.ts`。
    *   撰寫 DSR 認知模型公式（難度、穩定度、可回憶率的矩陣轉換公式），並提供一個 `calculateNextReview(card, rating)` 的核心方法。
*   **Step 1.2: 實作本地 RAG 檢索引擎**
    *   建立 `src/lib/searchEngine.ts`。
    *   實作基本的 `tokenize(text)` (斷詞)、`calculateTFIDF()` 以及 `cosineSimilarity()` 計算。

### Phase 2: 系統減法工程與狀態綁定 (Data Binding & UI Trimming)
在掛載新功能前，先大刀闊斧清理不需要的程式碼，並更新全域狀態。
*   **Step 2.1: 無情刪除多餘功能**
    *   刪除 `KnowledgeGalaxy.tsx` (知識星系圖) 與 `PresentationStudio.tsx` (簡報生成器)。
    *   清理 `App.tsx` 與 `TopNav.tsx` 裡面的相關路由與按鈕。
*   **Step 2.2: 升級 Memory Bank 資料結構**
    *   修改 `useMemoryStore.ts`。將原有的知識卡片，加上 `difficulty`, `stability`, `retrievability`, `lastReview`, `nextReview` 等 FSRS 必備屬性。

### Phase 3: 知識處理與應用層 (Knowledge & Application Layer)
實作全新的學習挑戰模式，讓知識被實際應用。
*   **Step 3.1: 擴充 AI 生成邏輯**
    *   修改 `src/lib/ai.ts`，新增 `generatePBLScenario(text)`。要求 AI 根據講義內容，生成一個「帶有角色扮演與實務困難」的情境任務。
*   **Step 3.2: 開發 PBL 挑戰介面**
    *   新增 `src/components/pbl/PblChallenge.tsx` 元件。
    *   提供一個輸入框，讓學生根據情境打出解決方案，並交由 AI 給予回饋。

### Phase 4: 動機與互動層強化 (Motivation & Interaction Layer)
將演算法與介面整合，達成終極的學習閉環。
*   **Step 4.1: RAG 檢索引擎與聊天室對接**
    *   修改 `ChatSidebar.tsx`。當學生發問時，先呼叫 `searchEngine.ts` 撈出講義中最相關的 3 個段落，再將這 3 個段落連同學生的問題，一起發送給 Gemini API。
*   **Step 4.2: 寵物狀態與 FSRS 綁定**
    *   修改 `PetCompanion.tsx` 與 `useAppStore.ts`。現在寵物的「健康度」與「情緒」，不再只是看「登入時間」，而是直接綁定 **「FSRS 系統中逾期未複習的卡片數量」**。逾期越多，寵物越慘！

---

## 驗證計畫 (Verification Plan)
1. **單元測試 (Unit Test 驗證)**：獨立測試 `fsrs.ts`，確認輸入「忘記」時，卡片的穩定度會下降；輸入「簡單」時，下次複習時間會成倍拉長。
2. **檢索準確度測試**：在聊天室隨便發問一個講義專有名詞，在 Console 檢查 `searchEngine` 是否真的精準抓到了對應的那一段。
3. **介面清潔檢查**：確認專案沒有任何懸空的路由 (Dead Routes) 或破圖。
